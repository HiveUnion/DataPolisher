pillow>=10.0
# Use the headless variant — removes Qt GUI backends (~100-150 MB in the bundle).
opencv-python-headless>=4.7
numpy>=1.26
paddlepaddle>=3.0
paddleocr>=3.0

# macOS only: OS-native Vision.framework OCR backend.
# When present, build_app.py skips bundling PaddlePaddle/PaddleOCR (saves 300-600 MB).
pyobjc-framework-Vision>=9.0; sys_platform == "darwin"
pyobjc-framework-Quartz>=9.0; sys_platform == "darwin"
